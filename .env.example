# No environment variables are required.
# The browser-only demo runs locally with bundled synthetic protocols and fixtures.
# Never commit secrets or company/internal configuration to this repository.
